class Program
 {
      static void Main(string[] args)
    int [] numbers = {1, 3 , 4, 546, 67 ,4};
        Console.WriteLine(numbers[0]);  // 1
        // 0 - 1 1 - 3
        int len = numbers.Length;
       Console.WriteLine(numbers[numbers.Length-1])
     }
